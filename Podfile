# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'multiple-select-table' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'R.swift'

  target 'multiple-select-tableTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'multiple-select-tableUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
